<nav class="bg-blue-600 shadow-lg">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <!-- Logo/Brand -->
      <div class="flex items-center">
        <%= link_to root_path, class: "text-white text-2xl font-bold hover:text-blue-100 transition" do %>
          🏘️ ParañaqueConnect
        <% end %>
      </div>
      
      <!-- Navigation Links -->
      <div class="flex items-center space-x-4">
        <% if user_signed_in? %>
          <!-- Logged In Navigation -->
          <%= link_to "Dashboard", dashboard_path, class: "text-white hover:text-blue-100 px-3 py-2 rounded-md text-sm font-medium transition" %>
          <%= link_to "Reports", reports_path, class: "text-white hover:text-blue-100 px-3 py-2 rounded-md text-sm font-medium transition" %>
          
          <% if current_user.admin? %>
            <%= link_to "Categories", categories_path, class: "text-white hover:text-blue-100 px-3 py-2 rounded-md text-sm font-medium transition" %>
            <%= link_to "Barangays", barangays_path, class: "text-white hover:text-blue-100 px-3 py-2 rounded-md text-sm font-medium transition" %>
          <% elsif current_user.resident? %>
            <%= link_to "Categories", categories_path, class: "text-white hover:text-blue-100 px-3 py-2 rounded-md text-sm font-medium transition" %>
            <%= link_to "Barangays", barangays_path, class: "text-white hover:text-blue-100 px-3 py-2 rounded-md text-sm font-medium transition" %>
          <% end %>
          
          <!-- User Menu -->
          <div class="flex items-center space-x-2">
            <%= link_to edit_user_registration_path, class: "text-white hover:text-blue-100 px-3 py-2 rounded-md text-sm font-medium transition" do %>
              ⚙️ Profile
            <% end %>
            <span class="text-white text-sm"><%= current_user.email %></span>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "bg-blue-700 text-white px-4 py-2 rounded-md text-sm font-medium hover:bg-blue-800 transition" %>
          </div>
        <% else %>
          <!-- Not Logged In Navigation -->
          <%= link_to "Home", root_path, class: "text-white hover:text-blue-100 px-3 py-2 rounded-md text-sm font-medium transition" %>
          <%= link_to "Log In", new_user_session_path, class: "text-white hover:text-blue-100 px-3 py-2 rounded-md text-sm font-medium transition" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "bg-white text-blue-600 px-4 py-2 rounded-md text-sm font-medium hover:bg-gray-100 transition" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>

